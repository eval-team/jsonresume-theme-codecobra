{{#if network}}
  {{#if username}}
    {{#is (lowercase network) "linkedin"}}
      {{> info-tag-small text=(concat 'in/' this.username) icon="fa-linkedin-square" }}
    {{else}}
      {{> info-tag-small text=this.username icon=(concat "fa-" (lowercase network)) }}
    {{/is}}
  {{/if}}
{{/if}}
